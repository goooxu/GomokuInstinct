# WORKLOG

> 本文档记录项目的开发过程与关键决策。**训练进度（loss、step 数、指标曲线等）不记入本文档。**

---

## 2026-07-25 — 项目立项与环境确认

### 立项

确定项目名 `gomoku-instinct`（Python 包名 `gomoku_instinct`）。名字来自项目最核心的约束：对战时不允许任何搜索，落子完全由一次网络前向给出，靠的是压进权重里的「棋感」。

### 需求约束（已与用户确认）

| 项 | 决定 |
|---|---|
| 训练数据来源 | 纯自博弈，不使用任何人类棋谱作训练或测试数据 |
| 模型结构 | 自行设计，不复用任何开源五子棋 AI 的结构 |
| 初始权重 | 全部从随机初始化开始，不使用任何预训练权重 |
| 数值精度 | 主权重为 BF16，混合精度训练 |
| 对战时推理 | 严格零搜索，单次前向 + argmax |
| 棋规 | 15×15 标准连珠禁手（黑：三三/四四/长连禁手；白无禁手，≥5 连即胜） |
| 开局 | 自由开局，不加 Swap2 等限定（规则层预留可插拔接口） |
| 禁手语义 | 可落子但即判负（严格 RIF），禁手点仍在动作空间内 |
| 训练期搜索 | 允许 MCTS 作教师并蒸馏进网络 |

### 关键设计判断

**1. 「无搜索推理」是整个项目的技术主线。**
AlphaZero 类方法的棋力一半来自网络、一半来自 MCTS。把 MCTS 从推理端拿掉后，原始策略网络通常会比它自己的 MCTS 版本弱很多。因此本项目的命题不是复刻一套自博弈 RL，而是尽可能把搜索能力完整地压进权重。为此在四个层面做了针对性设计：

- 网络走「深而窄」——用网络深度替代搜索深度；
- 辅助监督头（棋型、禁手点、剩余手数、对手应手）——把搜索过程中本该由树展开得到的信息，改为直接监督；
- 部署分布自博弈——一部分对局由零搜索策略执行落子，但训练目标仍由 MCTS 生成，消除训练/部署的分布漂移；
- 失误挖掘——定向采集「原始策略与 MCTS 判断分歧大」的局面加权重训。

评测门也必须用零搜索模式，否则会优化错目标。

**2. BF16 主权重需要补偿求和。**
要求是主权重本身为 BF16，而非常规的「FP32 master + BF16 autocast」。BF16 只有 8 位尾数，当 `lr*g` 相对 `w` 小于约 2^-8 时，`w += lr*g` 会被直接舍入丢弃，训练中期会彻底停滞。因此优化器实现 Kahan 补偿求和，把每步被舍掉的低位累积到补偿缓冲区，下一步补回；另提供随机舍入作为对照。Adam 的一/二阶矩保持 FP32（矩不是权重）。

**3. 禁手判定是唯一「写错了整个训练信号全错」的模块。**
三三禁手的 RIF 定义是递归的（一个三只有在「能一手成活四、且那一手本身不是禁手」时才算活三）。因此把规则引擎单列为一个里程碑，并设硬性验收门槛：一份高性能 C++ 实现 + 一份写法完全独立的 Python 参考实现做差分测试，外加一套按规则条文构造的边界用例。不通过不进入后续阶段。
> 这些用例是规则单元测试向量，由规则条文构造，不是棋谱，也不进入训练/测试数据。

### 环境确认

开发机为 aarch64 架构的 Grace-Blackwell 节点，4 卡全 NVLink 互联，两个 CPU socket 与 GPU 的 NUMA 亲和划分干净（前半 GPU 对应前半 CPU 核，后半同理）。原计划据此把 trainer 与 self-play actor 按 socket 物理隔离（后来只做了核数均分，未真正绑定，见 M6 的更正）。内存容量足以让 replay buffer 整个常驻，磁盘仅作容灾落盘。

因为是 aarch64，C++ 扩展需按 arm64 编译；统一在容器内构建，本机只做文件修改与 git 操作。

### 里程碑

- **M0** 仓库脚手架、容器环境、环境自检
- **M1** 连珠规则引擎（C++ + Python 参考实现 + 差分测试）
- **M2** InstinctNet 网络 + BF16-Kahan 优化器
- **M3** MCTS + 向量化自博弈 runner
- **M4** trainer + replay buffer + 完整 resume
- **M5** 9×9 小棋盘端到端验证
- **M6** 15×15 正式训练 + 竞技场晋级
- **M7** 无搜索专项优化 + CLI 人机对战工具

### M0 进展

- 建立仓库骨架：`configs/`、`csrc/`、`gomoku_instinct/`、`scripts/`、`tests/`
- `scripts/docker_run.sh`：容器内保持与宿主同一路径，以当前用户身份运行避免产生 root 属主文件；`HOME` 与 torch 扩展编译缓存都重定向到工作目录内，保证可重复构建
- `scripts/check_env.py`：环境自检，验证 GPU 可见性、BF16 matmul、C++ 工具链

自检顺带实证了 BF16 主权重方案的前提：朴素 BF16 累加在小步长下**完全停滞**（0% 的元素发生变化），
优化器必须做补偿求和，这一点不再是推测。

### M1 进展：规则引擎

按计划做了两份独立实现：

- **Python 参考实现**（`gomoku_instinct/rules/`）：抽出定长一维列表逐格扫描，优先可读性，是规则的可执行规范。
- **C++ 实现**（`csrc/`）：把每条直线打包成 `uint32_t` 位掩码，用窗口位运算与 popcount 判定棋型。
  越界不靠哨兵填充，而是靠「线本来就只有那么长」自然排除。

做法不同，结果一致才有意义。

#### 一处容易写错的地方：「四」怎么数

RIF 把「四」定义为「五格窗口内己方占四格、余一格为空」。照字面数窗口会出错——活四 `.XXXX.`
含有两个这样的窗口（左端补五、右端补五），会被误判成四四禁手，可活四明明是黑方的正常胜着。

真正的判据是**两个四是否共用同一组四颗子**：

```
.XXXX.     两个窗口的黑子集合都是 {1,2,3,4}          -> 同一个四 -> 活四，合法
X.XXX.X    黑子集合分别是 {0,2,3,4} 与 {2,3,4,6}     -> 两个四   -> 四四禁手
```

两份实现都按黑子集合去重（Python 用 frozenset，C++ 用位掩码），这两种形状因此能被正确区分开。
测试里为这条分界线专门写了一对用例。

#### 一处只有靠观测才能发现的问题：递归深度

三三禁手中「活三」的定义是递归的：一个三算活三，当且仅当存在一个空点，落子后能成活四，
**且那一手本身不是禁手**。递归每深一层都会往盘上多放一颗子，因此必然终止，深度上限只是安全网。

最初上限拍的是 8。差分测试第一次跑就报出封顶被触发 4 次——说明真实需要的深度超过 8，
取小了会让三三判定**悄悄退化成近似而不报错**。加上「实际最大递归深度」计数器后测得实际深度为 **11**，
遂把上限提到 64 并把该计数器纳入验收条件（封顶次数必须为 0）。

这类「静默降级」的 bug 不会让任何测试变红，只会让训练信号出现系统性偏差，所以规则层的审计计数器
是必须的，不是可选的。

#### 验收结果

| 项 | 结果 |
|---|---|
| 逐点判定比对 | 1.61 亿次（mixed 与 black_heavy 两种分布各约 8000 万），零不一致 |
| 禁手点覆盖 | 75.3 万个（长连 16.9 万 / 四四 27.1 万 / 三三 31.3 万） |
| 八重对称不变性 | 2.0 万次抽检，零失败 |
| 实际递归深度 | 11（上限 64，从未触顶） |
| 吞吐 | 约 350 万次判定/秒（128 进程） |

除差分测试外还加了一项**与实现无关**的不变性检查：规则在棋盘的八重二面体对称下必须完全不变。
这条性质不依赖任何一份实现，能独立抓出「把副对角当主对角处理」这类方向性 bug。

局面由四种随机分布生成（均匀撒子 / 聚集撒子 / 黑子占多数的密集局面 / 随机合法对局中途局面），
全部由程序生成，与人类棋谱无关；它们只作为规则判定的输入，不进入训练或测试数据。

### M2 进展：网络与 BF16 优化器

#### 四方向直线卷积的一个便利等价形式

主干每块都有一条四方向长核直线卷积分支，把「五子棋的一切都发生在直线上」写进架构。
朴素做法是用掩码 9x9 卷积模拟对角线，81 个抽头只用 9 个。

实际用的是另一种形式：把特征图右侧补 `radius` 列 gutter 再展平成一维序列后，
四个方向恰好变成同一条序列上四种不同 dilation 的 depthwise conv1d：

| 方向 | dilation |
|---|---|
| 横 | 1 |
| 竖 | Wp |
| 主对角 | Wp + 1 |
| 副对角 | Wp − 1 |

其中 `Wp = size + radius`。gutter 宽度恰好取核半径，保证跨行采样时越过行末的下标一定落进
补零区，而不会绕回下一行的真实数据。这比掩码卷积省一个数量级的算力。

下标推导差一格就会静默地把对角线采到别处，所以专门写了一份朴素的二维参考实现做逐点比对，
另有单独的用例验证 gutter 确实挡住了跨行串扰。

#### 注意力：从轴向改为全局

原计划用轴向注意力（行/列/双对角）。但 15x15 只有 225 个位置，全局自注意力的开销
与一个卷积块相当，实现更简单、表达力也更强（能建模任意两点间的关系，不限于同一条线），
因此直接改用全局的。

#### BF16 主权重踩到的两个坑

**其一：更新静默停滞。** 已在 M0 实证。优化器实现 Kahan 补偿求和，把每步被舍掉的低位
存进补偿缓冲区、下一步补回。测试里直接复现了对照：恒定梯度跑 1000 步，不补偿时权重
**一个元素都没动**，Kahan 补偿下正确推进到预期值。另提供随机舍入作为对照方案。

**其二：续训时优化器状态被静默降精度。**
`torch.optim.Optimizer.load_state_dict` 会把所有浮点状态**强制转成对应参数的 dtype**。
主权重是 BF16，于是 FP32 的一阶/二阶矩在续训时被悄悄降成 BF16 —— 探针里一步就把权重
从 1.0 打到 0.0，且不报任何错。开发机有使用时长限制、随时可能换机续训，这个坑必踩，
因此覆盖了 `load_state_dict`，按本优化器的约定还原各缓冲区精度，并加了对应测试。

#### 容器环境：uid 没有 passwd 条目会带塌 torch.compile

容器以宿主 uid 运行，而该 uid 在容器的 `/etc/passwd` 里没有条目，`getpass.getuser()`
抛 `KeyError`，导致 `torch._dynamo` 首次导入失败、重试时又撞上「重复注册」断言 ——
`torch.compile` 和 `torch.optim` 都会被带塌。`getpass` 优先读环境变量，
在 `docker_run.sh` 里给个固定用户名即可绕开。同时把 inductor / triton 的编译缓存
也收进工作目录内。

#### GB200 实测

默认规模 **20 blocks x 128 ch，4.44M 参数**。

| 配置 | 推理吞吐（batch 2048，BF16） |
|---|---|
| 初版 | 21,200 局面/s |
| 修正注意力张量连续性 | 26,100 局面/s |
| 再加 torch.compile | **52,400 局面/s** |

注意力那一处是：q/k/v 经 transpose 后最后一维 stride 不为 1，SDPA 会悄悄退回 math 路径，
把 (B, heads, N, N) 的注意力矩阵整个 materialize 出来，比 flash 路径慢一个数量级。
排成连续内存后单层从 6.64ms 降到 2.12ms。

通道宽度的取舍有实测依据：这个尺度下推理是访存受限，吞吐几乎与通道数成反比 ——

| 通道 | 参数 | 吞吐（compile 后） |
|---|---|---|
| 128 | 4.44M | 52,400 局面/s |
| 192 | 9.95M | 32,800 局面/s |
| 256 | 17.66M | 25,500 局面/s |

自博弈吞吐是全局瓶颈，而替代搜索深度的是网络深度不是宽度，所以加容量应优先加层数。
`channels_last` 布局在这个尺度下没有可观测差异。

### M3 进展：MCTS 与向量化自博弈

#### 一轮的节奏

不采用「一进程一局」，而是 C++ 侧同时推进上千局：

```
runner.collect()   每局从根下潜一次，停在叶子上，攒出一个批次的待评估局面
evaluator(...)     Python 侧编码特征、跑一次网络前向
runner.apply()     展开叶子、回传价值、退回根部；某局搜索次数够了就落子
```

这个安排带来三个直接好处：

**不需要 virtual loss。** 每局每轮只下潜一次，同一棵树上不存在并发下潜，
因此 PUCT 是精确的，不必引入 virtual loss 这种为了并行而做的近似。

**批大小恒定。** 等于同时推进的对局数，GPU 拿到的永远是同一形状的定长批，
可以直接被 CUDA Graph 捕获。终局叶子不需要评估，但仍占位以保持形状不变。

**下潜零拷贝。** 下潜时直接在真实局面上落子，apply 之后按记录的深度回退，
不复制任何 Position。

#### 特征编码只写一遍

runner 只吐出棋盘、行棋方、最近数手、手数这些原始数据，特征编码留在 Python
（`gomoku_instinct/model/features.py`）。编码规范因此只有一份，不会出现
C++ 与 Python 两边悄悄走样、而训练与自博弈各用一套的情况。
每轮传输量只有 `对局数 x 棋盘格数` 字节，可以忽略。

#### 几处需要拿捏的细节

- **行棋方的语义要统一。** `Position::play` 无论是否终局都切换行棋方，
  `to_move` 恒为「下一手轮到谁」。搜索树用它确定叶节点的价值视角，
  含糊一点符号就反了。
- **先验要在合法着法上重新归一化。** 调用方只按空点做屏蔽；ILLEGAL 语义下
  禁手点也不合法，落在那些点上的概率质量必须摊回来。
- **每手重建搜索树，不做子树复用。** 换来的是节点池占用恒定（每手最多新增
  sims 个节点），实现也简单。子树复用是后续可选的优化项。
- **线程之间零共享。** 每局的统计量与产出样本都存在各自的槽位里，
  工作线程互不接触，汇总只在主线程做，全程无锁。
- **随机数状态完整序列化。** 用标准库的流序列化导出每局 RNG 的完整状态，
  换机续训后随机流逐位一致。

#### 测试里冒出来的一个有意思的现象

原本写了一条测试，断言「随机自博弈中黑方会踩到禁手点」，结果恒为 0。

原因不是 bug：**MCTS 自己就把禁手点规避掉了**。禁手点的子节点是即时负，
访问一次拿到 -1，PUCT 立刻就不再选它 —— 哪怕先验是均匀的、网络什么都不懂。
这恰恰是我们希望蒸馏进策略网络的行为。

于是拆成两条测试：带搜索时禁手告负率必须很低（<5%），而把搜索摘掉、
纯随机落子时必须大于 0。前者验证搜索确实在规避，后者验证「禁手点仍是合法落子、
落上去立即判负」这条语义真的接在 runner 里 —— 只看其中一条都会得出错误结论。

#### 关于「局/秒」这个指标的陷阱

上千局同时推进时，单局要跑很久才结束：1024 局并行、总计 240 手/秒，
单局只有 0.23 手/秒，一局三十多手要两分多钟。测量时长不够时「局/秒」严重失真
（90 秒的测量只跑完 1 局）。所以基准脚本以**手/秒**为主指标，
局产量由「手/秒 ÷ 平均每局手数」推算，并在完成局数不足时明确标出不可信。

#### 单卡实测

15x15，1024 局并行，36 个搜索线程，sims 400/100、完整搜索比例 0.25
（即平均 175 次评估/手）：

| 指标 | 数值 |
|---|---|
| 网络评估 | 41,905 局面/s |
| 落子 | 239.5 手/s |
| 完成对局 | 2.76 局/s（平均 87 手/局） |
| 训练样本 | 40.5 条/s |
| GPU 前向占比 | 84.8% |
| CPU 树搜索占比 | 15.2% |

平均 87 手/局是**随机初始化网络**下的数字，双方都不会主动成五，对局拖得很长；
网络练起来之后每局手数会显著下降，局产量随之上升。三张卡跑 actor 即约 8 局/s 起步。

当前是 GPU 受限（前向占 85%），CPU 侧还有余量 —— 后续若要提产量，
优先方向是降低单次前向成本（CUDA Graph、更激进的编译），而不是加搜索线程。

顺带一个关于棋规的观察：随机网络下 **白胜 643 / 黑胜 518**，白方反而占优。
原因是黑方被禁手约束且必须**恰好**五连，而白方五连及以上都算胜。连珠的禁手规则
本来就是用来抵消黑方先手优势的，在棋力很弱时这个约束反而压过了先手的好处。
同期禁手告负为 0，再次印证搜索确实在规避禁手。

#### 棋型标注器

辅助监督头的标签由 `Rules::pattern_map` 产出：对每个空点给出该点落子后能形成的
最高棋型等级（眠三/活三/冲四/活四/五连/长连）。

这里的「活三」刻意用**非递归**判定（只看能否一手成活四），与三三禁手里那个递归定义不同 ——
它只是个特征标签，递归版本要贵一个数量级。精确的禁手规则交给单独的 forbidden 头去学，
标签由 `forbidden_map` 提供，那一份是严格递归的。这样既保住了标签的精确性，
又没让特征标注拖慢数据流水线。

### M4 进展：训练循环、replay buffer 与续训

#### 辅助标签算在入库时，不算在训练时

棋型与禁手点都是棋盘的确定性函数，一开始想在取 batch 时现算。估了一下不行：
一个局面的全盘标注约 50ms，batch 1024 就是 51 秒 CPU，摊到 144 核也要 0.36 秒 —— 
而训练一步只要 0.2 秒。

改成入库时算一次：样本产出速率只有 40 条/秒，每条算一次总共占几个核；
而每条样本在窗口里会被训练几十次，算一次存下来显然划算。存储代价约 675 字节/条，
400 万条窗口不到 3 GB，相对内存容量可以忽略。

#### 容灾：保存的不只是权重

按 CLAUDE.md 的要求，任何时刻被 kill 都要能原地续训。checkpoint 里存的是：

- step / cycle / 已训练样本数
- 模型权重（BF16）
- 优化器状态，**含 Kahan 补偿缓冲区** —— 漏掉它就会丢掉已经攒下、尚未进位的低位更新
- Python / NumPy / Torch / 自博弈引擎四套随机数状态
- replay buffer 的分片游标

checkpoint 与分片都是「先写临时文件再 rename」，`latest` 指针原子更新，
续训时读到的一定是完整文件。触发条件按**墙钟时间**（默认 10 分钟）而非只按 step，
把抢占损失控制在有界范围内。

测试里模拟换机（全新进程、只共享 run 目录），逐项比对恢复后的 step、权重、
Kahan 补偿范数、buffer 规模，而不只是「加载没报错」。

这个测试当场抓到一个 bug：`total_added`（历史产出样本总数，用来算样本复用率）
在续训时被重复计了一遍 —— `load_state_dict` 先按 checkpoint 设了值，
`restore_from_shards` 又把装回来的样本当成新产出累加了一次。
不影响训练正确性，但会让复用率指标虚低一半，进而误导「该多跑自博弈还是多跑训练」的判断。

#### 策略损失里的一个陷阱

非法点的 logit 被填成 `-inf`，而目标概率是 0，直接相乘得到 `0 * (-inf) = NaN`，
整个 loss 就废了。目标在这些点上本来就是 0，把对应的 log 概率置零即可，
损失值不变。这个坑不写测试很容易漏 —— 它只在「目标分布恰好在某些位置为 0」时触发，
而这正是屏蔽掉的位置。

#### 指标选择

几个指标是刻意挑的，因为默认的那些会误导：

- **禁手点看召回率与精确率，不看准确率**。禁手点极其稀疏，全判「不是」也能有 99% 准确率。
- **棋型看「真有威胁的点」上的准确率**。全盘绝大多数点是 NONE，整体准确率会被稀释。
- **样本复用率**（已训练样本数 ÷ 历史产出样本数）。太高会过拟合，太低是浪费自博弈算力，
  这是调节自博弈/训练配比的直接依据。
- **Kahan 补偿缓冲区范数**。它稳定大于 0 就说明确实有更新量正在被 BF16 舍入吃掉、
  又被补偿救了回来 —— 可以直接看出主权重 BF16 有没有开始拖后腿。

### M5 进展：9x9 端到端验证

跑通了完整闭环（自博弈 → 入库 → 训练 → checkpoint → 对战），并且确认网络确实在学：
价值损失持续下降、价值准确率从 55% 升到 76%、每局手数从 54 降到 46（会赢之后棋局变短）。
Kahan 补偿范数稳定在 0.17 左右且非零 —— 补偿机制一直在实打实地救回被舍掉的更新。

#### 抓到的问题：自博弈与训练的配比会自己跑偏

首轮验证里**样本复用率飙到 23.8**，即每条样本被反复训练二十几遍，已经进入过拟合区间。

根因是「自博弈跑几轮、训练跑几步」这种固定配比本身就不稳：自博弈一手棋要几百次
网络评估，而一个训练步只吃一个 batch，两者产出/消耗速率差好几个数量级，
而且这个比值还随棋力变化（棋力上升 → 每局手数下降 → 样本产出速率变化）。

改成按**目标复用率自适应**：每个周期先算「按目标复用率还允许训练多少样本次」，
再折成步数，用不完就不训练、等自博弈补数据。这样不必手工去配两个常数，
棋力变化时也能自己跟上。

#### 棋力标尺

训练 2880 步后的零搜索策略：

| 对手 | 结果 |
|---|---|
| 随机落子 | 98胜 2负（Elo +676），平均 10 手结束 |
| 规则基线（贪心威胁） | 0胜 100负（Elo −920） |

这个对比正是设置绝对标尺的意义：只看「打败随机」会误以为已经不错，
而对上一个只会「能赢就赢、该挡就挡」的规则基线就原形毕露。Elo 是相对量，
没有固定参照物根本看不出真实水平。

另外对随机的 100 局里有 **2 局是自己走出禁手告负** 的。这正是本项目要盯的短板指标 ——
在严格 RIF 语义下引擎不替模型屏蔽禁手点，避开禁手是它必须自己学会的能力。

### M7 进展：命令行对战工具

`gomoku-instinct play` 已可用：棋盘渲染带坐标，最后一手用不同字形标出（加括号会破坏列对齐），
人执黑时用 × 标出禁手点，支持 undo / hint / save / load / resign。
`--show-policy` 会显示 AI 的候选手概率、价值估计与它自己对禁手点的预测。

**AI 落子严格是「一次前向 → 屏蔽已占点 → argmax」**，没有树搜索、没有 rollout、
没有开局库。默认连禁手点都不替它屏蔽（`--safe-mode` 可显式开启规则兜底）。

另有 `arena` 子命令做批量对局与 Elo 估计，`show` 查看 checkpoint 信息。
竞技场里模型一律以零搜索模式参赛，与部署条件完全一致 ——
用 MCTS 模式评测晋级会优化错目标。先后手逐局强制轮换，因为连珠黑白规则不对称，
不轮换的胜率没有可比性。

顺带修掉一个容器脚本的坑：`docker run` 不加 `-i` 时根本不会把 stdin 接进容器，
用管道给对战 CLI 喂着法会立刻读到 EOF。原来只在有 TTY 时才加 `-it`，
现在 `-i` 恒加、`-t` 仅在有终端时加。

### M6 进展：多卡编排

自博弈从 trainer 进程里拆出来，做成独立的 actor 进程，各独占一张 GPU：

```
GPU 0   trainer   扫描新分片 -> 训练 -> 写 checkpoint
GPU 1-3 actor     热加载最新权重 -> 自博弈 -> 写分片
```

两侧之间**除了文件系统没有任何耦合**：actor 定期看一眼 `checkpoints/latest`，
有新权重就热加载，不需要握手；样本分片是原子 rename 出来的，
所以 trainer 扫到的一定是完整文件，也不需要额外的协议。

这个松耦合直接换来了容灾性质：任一侧崩掉，另一侧照常跑；重启后各自从磁盘上的
最新状态接上。换机时把进程停掉、在新机器上跑同一条启动命令即可 ——
工作目录路径两机一致，checkpoint 与分片都在原地。

每个 actor 一套独立的随机流，否则几张卡会跑出一模一样的对局。

（**更正**：早先这里写的是「资源按 NUMA 亲和切分、同一 socket 上的 GPU 与 CPU 核绑在一起」，
与实际不符 —— 启动脚本只把核**数量**均分给各 actor，并没有调用 numactl 做绑定。
后来的流水线剖析显示 actor 侧 96% 的时间花在 GPU 前向、CPU 树搜索只占 3.3%，
NUMA 绑定因此不是当前瓶颈，但把它写成已完成是不对的。）

启动后实测：三张 actor 卡利用率 82~86%（关闭认输后到 100%），
trainer 卡在样本攒够之前保持空闲（`min_positions_to_start` 未达成时不训练，
这是预期行为）。

#### 正式训练一开跑就暴露的问题：未训练的价值头 + 认输 = 数据污染

首次启动后监控到**黑方胜率 0%**（后续 611 局里黑方只赢 13 局）。M3 的基准测试
同样是未训练网络，当时是黑 518 / 白 643，黑方 44.6%。两者差得离谱，必须查清楚。

先排除了一个错误猜测：我一度以为差异来自认输开关，但翻回去看，M3 基准用的是
`SelfPlayActor` 的默认参数，认输本来就是开着的。真正的唯一差异是
`raw_policy_fraction`（0 vs 0.25）。于是做了受控 A/B，三张卡并行各跑 420 秒：

| 配置 | 黑胜 | 白胜 | 黑方胜率 |
|---|---|---|---|
| raw=0，认输 off | 17 | 16 | 51.5% |
| raw=0.25，认输 off | 50 | 26 | 65.8% |
| raw=0，**认输 on** | 26 | 49 | 34.7% |

结论反而落回认输：它确实把结果系统性推向白方。而生产跑之所以更极端（黑方 2%），
是因为**随机初始化的价值头的偏向本身是随机的** —— 这次的初始化恰好对
「轮到黑走」的局面输出极低值，于是黑方从第 10 手起就无脑认输，
完成局平均只有约 17 手。同一份代码换个随机种子，症状可能完全不同，
这比固定偏差更难察觉。

处理：训练初期默认关闭认输，等价值头有了准度再打开，打开时盯住 audit 出来的
误判率（`resign_false_positives / resign_audits`）。审计机制本来就是为此建的，
这次是它该发挥作用却没被读到 —— 日志里压根没打印这两个数，于是补上了。

#### 一个一直在误导的指标

排查过程中还发现「手/局」这个指标从一开始就是错的：分子是**总落子数**
（包含上千局仍在进行中的对局），分母只数**已完成**的对局。上千局同时推进时，
在飞对局贡献了绝大部分落子，于是 17 手的对局被报成 84 手 ——
正好掩盖了「对局异常短」这个本该第一时间暴露认输问题的信号。

C++ 侧补了 `completed_plies` 只统计已完成对局的手数，所有报告改用它。

### M6/M7 收尾：一个否定性结论

15×15 训练跑到学习率余弦衰减收敛后停下。这一阶段最有价值的产出不是某个数字，
而是一个**否定性结论**，它排除掉了一整类改进方向。

#### 训练侧与棋力侧脱钩

诊断用的量是「策略损失减去目标分布自身的熵」，也就是网络离教师还有多远。
它从起点降到收敛点，降幅约四分之一；同期策略 top1 一致率、价值准确率、
威胁识别都在同步改善，威胁识别接近饱和。

**但头号指标（零搜索策略值多少次搜索）在同一段训练里基本没动。**

这意味着：把 MCTS 的访问分布拟合得更准，并不转化为零搜索时更会下棋。
而「加大模型」「提高教师 sims」「继续降学习率」这些手段，本质上都只是让拟合更准。
按这个证据，它们都不会撬动头号指标。

我的解读：蒸馏访问分布，学到的是**教师最后选了哪里**，而不是**教师为什么这样选**。
MCTS 的强项是逐个验证候选手的后果，这个验证发生在搜索**过程**里，不在最终分布的
形状里。网络的威胁识别已接近满分——它看得见威胁，但看不见「我走这里之后对手会怎样、
再之后我会怎样」。

若要继续推进，方向应当是**换监督目标**：把搜索过程中的中间量（例如「走 X 之后
对手的最佳应手及其价值」的多步展开）也作为监督信号，而不是只蒸馏最终分布。
这需要在 C++ 侧导出搜索中间量、加新的头与损失，是一项中等偏大的改动。

#### 几次判断失误的记录

学习率实验中我连续两次没估准趋势：先是只看了 1,550 步就断言「改善有限、不像
优化受限」，后来按学习率分箱看出单调下降、又外推出偏悲观的收敛点。教训有两条：

- 这类量的单点噪声（sd 约 0.02）与真实趋势（每次学习率减半约 0.01~0.02）同量级，
  **必须分箱统计，看单点必然误判**；
- 「指标不动」有两种截然相反的成因——学不动，或已学完。绝对损失分不开这两者，
  减去目标熵才分得开。这个量我一直在记录，却直到卡了一万多步才想起来看。

#### 已知未竟

- **搜索差距**：零搜索策略目前约相当于个位数次模拟的 MCTS。压缩它是本项目的原始命题，
  尚未解决，且已确认不能靠拟合精度解决。
- **禁手告负率回升**：从低点单调爬升了数倍（绝对值仍远低于早期峰值），与黑方胜率
  升高同步——进攻越频繁，踩禁手的机会越多。未深入排查。
- **CUDA Graph 未实现**：流水线剖析显示 actor 侧 96% 的时间花在 GPU 前向上，
  这是提高自博弈产量最直接的方向。
- **价值头在开局一路判「白优」**，与自博弈中黑方胜率更高对不上。做网页版时顺手验过
  符号：造四个胜负已定的局面（黑/白各持活四、分别轮到己方与对方），四个都判对，
  **不是符号反了**，是模型对开局的真实判断。值得单独查一次。
  顺带暴露一个盲区：**零搜索选点只用策略头，价值头根本不参与决策** ——
  它就算整个坏掉，竞技场、Elo、禁手告负率也全都照常，现有指标一个都发现不了。

### M7 补充：网页版对战

命令行棋盘看久了累，加了个网页版：`gomoku-instinct serve`。

**服务端只用标准库的 `http.server`。** 这个项目至今没有 torch 之外的运行时依赖，
为一个试玩页面引入 Web 框架不划算，容器里也未必装得上。落子决策与 CLI 完全共用
`InstinctPlayer`，仍然是「一次前向 → 屏蔽已占点 → argmax」，换的只是界面。

界面上真正解决「看着累」的几处：

- 鼠标悬停时在落点画半透明的棋子、拉出十字准星、四边坐标同步高亮，落子前就确认；
- 上下左右四边都标坐标，不必把视线拉回一侧去数行列；
- 棋谱可点击回看任意一手 —— 局面由前端按手数前缀重建，不必回服务端要。

两个设计上的取舍：

- **评估值必须连同「站在哪一方视角」一起返回。** 提示（轮到人时算）与 AI 自评
  （轮到 AI 时算）正负号含义正好相反，只存一个数字的话界面上没法解释，
  而一个符号讲反的胜负条比没有还糟。
- **禁手点只发给执黑的人**，与 CLI 一致。人执白时把 AI 的禁手点摊开，
  等于替它把风险标出来。

**默认只监听回环地址。** 页面没有任何认证，而开发机通常是共享的；要从别的机器
访问就用 SSH 端口转发，`--host 0.0.0.0` 需显式指定。静态文件加了目录穿越防护——
本地工具也不该能读到静态目录之外。

测试用假 player 起一个真的 HTTP 服务、走真实请求：这些接口的价值全在「浏览器发来
一个不合规矩的请求时会怎样」，直接调函数测不出来。覆盖了已占点、越界与非整数落点、
终局后落子、会话失效、会话隔离、禁手点判负、人执白时的悔棋补手、目录穿越。

#### 一个 70 倍的静默性能坑：推理不能跑在请求线程上

上线后实测一次落子要 **810ms**，而单次前向只要 11ms。逐层排除下来：

| 位置 | 耗时 |
|---|---|
| `analyze` 在主线程 | 11 ms |
| `analyze` 在一条复用的工作线程 | 141 ms |
| `analyze` 在每次新建的线程 | 800 ms |

`ThreadingHTTPServer` **每个连接开一条新线程**，而 cuBLAS/cuDNN 的句柄是**按线程**
建的 —— 每次落子都在重建句柄。把推理统一赶到一条长期存活、启动时预热过的线程上
（`ThreadPoolExecutor(max_workers=1)`），落子回到 **23ms**，快了三十多倍。

这个坑的形状和本项目反复遇到的那一类完全一致：**不报任何错，只是慢，而且慢得
像是"模型就这么大开销"，很容易被当成理所当然接受**。中间还走了两条错路——先怀疑
是 torch 开满 144 个 OpenMP 线程自旋抢 CPU（改线程数无效），又怀疑是 HTTP 层
（最小 http.server 对照只要 0.4ms，排除）。每一步都靠量，不靠猜。

顺带把静态文件按 mtime 缓存进内存：工作目录在 NFS 上，每次请求重读一遍要 30ms。

#### 前端：render() 把用户刚选的颜色抹掉了

用户报「没办法执白」。原因是 `guard()` 在发请求前会先 `render()` 一次，而 `render()`
里有一句把颜色下拉框回写成当前对局的颜色 —— 等到真正读 `$("color").value` 时，
用户选的「白」早被覆盖回「黑」了。点下去不报错，只是又开了一局黑棋，必现。

根子是**把输入控件当成状态显示去写**。改法不是绕过去，而是让 render() 不再碰任何
输入控件；同时颜色一改就直接开新局（中途换色本来就没有意义），已落子的先确认。

顺带查出同一处的第二个 bug：会话失效（服务重启过）时的恢复路径调的是 `newGame()`，
而它走 `guard()`，此刻 `pending` 已经是 true，`guard` 开头就 return —— 新局根本没开
出来，用户卡死在一个已失效的对局上，同样不报错。

**容器里没有 JS 运行时，前端跑不了真正的单元测试。** 不为一个试玩页面引入整套 JS
工具链，改为把这两个 bug 写成静态不变量来挡复发：render() 的函数体里不得出现对
`$("...").value / .checked` 的赋值；`api()` 的函数体里不得调用 `guard()`。
（写第一版时这两条检查匹配到了解释 bug 的注释文字而误报，加了去注释这一步。）
这挡不住所有前端问题，只挡住已经踩过的这两类 —— 覆盖缺口是真实存在的。

后来找到一个能补上一大块缺口的办法：**纯 Python 的 esprima 可以做 JS 语法检查**。
语法错误的后果是整页白屏，而服务端一切正常、日志里什么都没有，只能靠打开浏览器
才发现 —— 这正是最值得自动挡住的一类。已加进 `setup_container.sh` 与测试
（没装 esprima 时自动跳过）。它解析到 ES2017，用了更新的语法会在测试里报错
而不是在浏览器里。

### M7 补充：光标式落子

用户反馈实际对战时「落子位置很容易看错」——15×15 上靠肉眼数格子把点位翻译成
坐标再敲进去，错行错列是常态。改成方向键选点：光标在棋盘上反显，当前坐标与
该点状态（空点 / 你的禁手点 / 已有子）实时显示在下方，落子前就能确认。

三个必须处理的细节，都是「不处理就静默不工作」那类：

- **方向键不是单个字符**，而是 `ESC [ A/B/C/D` 转义序列。读到 ESC 后要看有没有
  后续字节，但不能阻塞读——用户单按一下 Esc 会把程序卡死。用 select 给一个很短的
  超时来区分「转义序列」与「孤立的 Esc」。
- **raw 模式下 Ctrl-C 不再产生 SIGINT**，只是一个普通字节 0x03。不显式处理用户就退不出去。
- **退出时必须还原终端属性**，否则用户的 shell 会失去回显和行编辑，只能 `reset` 救回来。
  还原用的是进入前保存的属性——中途需要输入文件名时，如果现读当前属性再「还原」，
  还原到的还是 raw 模式，等于什么都没做（写的时候就踩了这个坑）。
- **`tty.setraw` 默认是 `TCSAFLUSH`，会把已排队的输入直接丢掉**。程序刚启动那一瞬间
  敲的键会凭空消失，不报错、不提示。改用 `TCSADRAIN` 显式保留。这个坑是靠测试
  无声死等暴露出来的——手工试根本不会发现，因为人不会在启动瞬间按键。

光标用**反显**而非加括号：括号占额外列宽会把整行对齐搞乱，而棋盘一旦错列，
「看错落子位置」这个本来要解决的问题反而更严重。测试里专门断言了
「去掉 ANSI 转义后与无光标渲染逐字符相同」。

非终端环境（管道、重定向）自动退回原来的坐标输入模式，脚本化用法不受影响；
`--input-mode` 可以显式指定。

测试用真 pty 驱动整个交互循环，而不是给按键读取打桩——这块代码的价值恰恰在
终端交互本身。写测试时先踩了一个坑：**pty 从端默认是行缓冲（canonical）模式**，
写进去的字节要等换行才对读端可见，于是 `os.read` 直接死等。测试里必须先 `setraw`。

还有一次自我纠正：光标模式每按一次键都要重画，而重画会重算一遍全盘禁手（纯 Python
参考实现），我据此加了一层按局面版本号失效的缓存。实测下来单次只要约 12ms，
肉眼完全无感——之前误以为它慢，是把首次 JIT 编译 C++ 扩展的耗时算到了它头上。
缓存遂被撤掉：它换来的是零收益，加上「以后每处改动局面的代码都必须记得让缓存失效」
这一类正是本项目反复吃亏的静默失效隐患。**先量再改**。

### 补充：技术报告

在 `docs/` 下写了一份面向 PyTorch 初学者（无强化学习背景）的技术报告，12 章 + 导读，
配 14 张手写 SVG。

与既有文档的分工：README 讲「怎么用」，本文档讲「按时间发生了什么」，
报告讲「为什么这么设计、原理是什么」，按主题而非时间组织。三者尽量不互相复制，
需要背景时互相引用。

报告里两章是这份 WORKLOG 的内容重新组织而来，但视角不同：

- **第 11 章「静默失败图鉴」** 把散落在各里程碑里的十四个「不报错、只是悄悄不工作」
  的 bug 收拢成一章，统一按「症状 / 根因 / 怎么发现的 / 怎么防复发」写，
  末尾归纳出四种应对手段（审计计数器、双实现差分、对照实验、把坑写成回归测试）。
- **第 12 章「一个否定性结论」** 把训练侧与棋力侧脱钩这件事完整讲一遍，
  包括它排除了哪三条改进方向、下一步该往哪走，并诚实标定棋力（只相当于 4~8 次模拟）。

工程上加了 `tests/test_docs.py`，把几条能机器验的约束固定下来：相对链接与图片目标
必须存在；SVG 必须能解析且有铺满 viewBox 的背景矩形（否则 GitHub 暗色模式下不可读）；
文档里提到的仓库内路径必须真实；每章末尾「代码索引」表里的符号必须真的在那个文件里。
最后一条是防文档腐烂最有效的 —— 改了类名而没改文档，测试会红。

还有一条 `test_no_dev_machine_details`，用通用模式（任意 IPv4、任意 `/home/` 绝对路径）
检查文档不泄漏开发机信息，这样测试本身也不会把要保护的东西写进仓库。

**手写 SVG 是盲写的**，所以最后把 14 张图都渲染成 PNG 实际看了一遍，
修掉两处肉眼才能发现的问题：一处图例标签跨过了分栏分隔线与符号重叠，
一处坐标标签被右侧说明框压住。**布局类的东西，不看渲染结果就等于没验证。**

---

## 2026-07-29 — 换机、工具修正与网页版增强

### 换机重建环境

多个 run 之间不会互相干扰：run 目录是唯一的耦合点，连 docker 容器名前缀都由它派生
（`launch_training.sh` 里的 `TAG`），所以同一台机器上并存也没问题。

换开发机时环境要从零建：拉镜像、装依赖、编译 C++ 扩展。有两样东西因为当初就放在
工作目录内而直接复用了：

- Python 依赖装在 `runs/.container_home/.local` 下（`docker_run.sh` 把容器 `HOME`
  指到那里），换机后 `setup_container.sh` 跑完全是 "Requirement already satisfied"；
- C++ 扩展与 inductor/triton 缓存在 `build/` 下。

`docker_run.sh` 里那个「项目根目录一变就清空编译缓存」的标记文件这次起了反向作用：
路径没变（两台机器上工作目录映射为同一路径），缓存判定为有效，省掉一次完整重编。
**当初把 HOME 和编译缓存塞进工作目录，本意是别往工作目录外写文件，换机时顺带白赚了一次。**

新机器环境自检全部通过：4 卡、aarch64、BF16 支持正常、朴素 BF16 累加在小步长下
仍然完全停滞（即 Kahan 补偿依然必要）、C++ 工具链就绪、144 核。全量测试 242 个用例通过。

### 两处改动

**重写了 `max_steps` 的配置注释。** 原注释记的是当初把它从 200 万改到 3 万那次实验的
背景，但更要紧的是另一件事：**这个值是学习率余弦衰减的分母**，
不是「跑到哪算哪」的上限。中途调大它，学习率会从谷底跳回半山腰。想延长训练必须知道
这一点，所以把这句写进了注释。

**`metrics.jsonl` 每条记录加 `wall_time`。** 这是技术报告附录 A.1 里自己记下的疏漏：
上一轮没记墙钟时间，等自博弈分片被清理掉之后，「训练实际跑了多久」就再也查不回来了
（原本只能从分片文件的 mtime 反推）。一行 `setdefault` 的事，早该做。

### 一个不必再管的旧坑

顺带确认了报告第 11 章 #15（重启后回放池归零、在容量 1.25% 的池子上过拟合 150 步、
三项训练指标反而冲到全程最佳）这次不会重演。

当时的根因**不是** `min_positions_to_start` 这道绝对值门槛本身 —— 续训路径上本来就有
`restore_from_shards()`，会从磁盘分片把回放池填回去，门槛根本不该被触发。真正的原因是
#8：`_read_shard` 把旧分片的 `KeyError` 吞掉了，导致所有分片被静默跳过，回放池什么都没
装回来，门槛才显得形同虚设。**两个静默失败叠在一起，才凑出了那个「指标变好」的假象。**

#8 已修（分片读取对缺失字段填零并明确报告跳过数）。只要一个 run 的分片由同一份代码
写出、字段一致，重启就会正常重建回放池。

### 又一个静默失败：性能剖析工具测的不是生产配置

`scripts/profile_pipeline.py` 一直在剖析**未编译**的模型，而 actor 与 trainer 在生产里
都开着 `compile: true`（`scripts/actor.py` 里那行 `torch.compile(model) if tcfg.compile`）。
于是它报出来的耗时系统性偏高一倍以上，**不报错、不崩溃，数字看着完全合理**。

发现过程值得记：先是发现实测吞吐比按 profiler 单轮耗时外推的结果快 2.2 倍，
第一反应给了两个"听起来很有道理"的解释（同步开销、树还空着导致批量偏大），
但都没验证。去读代码才找到真因，两侧的倍数还能互相印证：

| | profiler（未编译） | 实测（编译后） | 倍数 |
|---|---|---|---|
| trainer 一步 | 0.342 s | 0.137 s | 2.5× |
| actor 一轮 | 83.83 ms | 38.9 ms | 2.2× |

trainer 那个 0.137 s 是**从 `metrics.jsonl` 直接读出来的**，没有另外测：有一段
`train/steps_this_cycle` 恒为 222、`buffer/size` 不变，说明 trainer 在连续训练、
完全没等数据，除一下墙钟差就是纯计算耗时。这正好是今天新加的 `wall_time` 的第一次派上用场
—— 加它的时候只是想着"别再丢失训练时长"，没想到它顺带让"算力占比"变成可以事后复算的东西。

为什么编译能快一倍多：这个模型又小又深（4.44M 参数、20 个 block，每个 block 是一串
小算子），batch 2048×225 的张量并不大，**瓶颈是 kernel 启动开销与显存往返而不是浮点量**，
逐元素算子一融就见效。换个以大矩阵乘为主的模型，编译大概只能挤出几个百分点。

修法：profiler 两侧都按 `cfg.compile` 决定是否编译，并在抬头打印当前是开还是关。
**一个专门用来测性能的工具，默认测的却不是生产配置** —— 这类错误不会有任何征兆，
只会让人照着错的数字去优化错的地方。

顺带记一个方法论上的自我纠正：我最初按 profiler 的数推算"10000 步要 6.8 小时"，
实测是约 2.8 小时。单轮耗时用来看**各步骤占比**是有效的，用来推**吞吐**是无效的。

### 网页版：跟着训练走、多局并行、每局选不同模型

训练跑起来之后陆续加的几件事，按提出的顺序：

**热加载。** 原来只在启动时载一次权重，想看训练进展只能反复重启。加了
`--reload-seconds N`，指向 run 目录时每隔 N 秒检查 `checkpoints/latest` 有没有变。
两个容易出静默问题的地方：重新载入必须**排到那条专属推理线程上**（否则会和正在
进行的前向抢同一个模型）；`meta` 要整个 dict 一次性重新绑定而不是逐字段改
（`state()` 读它时没加锁，逐字段改会被读到一半新一半旧）。

**多模型 + 每局各自选。** `--ckpt` 改成可多次指定。这里踩了一个坑，值得单独记，
已写进技术报告第 11 章 #17：最初"当前用哪个模型"是**服务端全局状态**，而页面
缓存了一份快照，我又在 `render()` 里把 `S.step` 写回了那份缓存 —— 于是
某个模型顶着**另一个模型**的 step 显示，且不会自己恢复。

这和 #11（`render()` 抹掉用户刚选的颜色）**是同一个模式**，当时还专门写了静态不变量
测试挡它。但那条测试只检查"不得回写 `<select>`/`<input>` 的 value"，
没覆盖"写回缓存的数据结构"。**回归测试写得比 bug 本身窄，就只能挡住那一个 bug，
挡不住那一类。**

修法分两层：表层让"哪个模型在用"和"它练到哪一步"出自同一份数据；根子上把模型下标
挪进 `Session`，变成每局各自持有 —— 顺带就实现了"多局并行、每局不同模型"。
页面用 `sessionStorage`（**不是** localStorage，后者所有标签页共用一个 sid，
多开反而变成同一局）记住本标签页的对局，刷新不丢棋。

还有一个我自己的验证失误：第一版端到端验证是"A 局用模型 0、B 局用模型 1，把 A 切到 1"
—— 两边都是 1，差点当成通过。可 B 本来就是 1，**这个断言恒为真，等于没有断言**。
重做成"两局都从 0 开始、只切一局"才真正验证了独立性。

**候选点按可调阈值显示。** 原来固定标第 2、第 3 高的两个，改成标出概率不低于阈值的
全部候选，阈值由滑杆调（1%~50%，默认 5%）。服务端把 top_k 提到 100：阈值最低 1%，
而概率大于 1% 的点最多 99 个，所以任何允许的阈值都不会因为服务端截断而漏掉候选。
**不做"取前 N 个"式的静默截断** —— 截断了却不说，看到的人会以为模型只考虑了这几个点。

**排布按棋盘面积自动切换。** 去掉了 media query。该不该上下排布取决于"哪种排法棋盘更大"，
而那要同时看窗口的宽和高，CSS 里算不出来，断点拍在哪都会在某些窗口尺寸上选错
（比如宽 900、高 400 的窗口，左右排布的棋盘反而更小）。改成 `resize()` 里实测两种
排法各能给出多大的棋盘，取大的那个。

顺带修掉一个一直存在的布局 bug：`resize()` 里写的是 `px = Math.max(280, avail)`，
把画布抬到固定下限，而容器可能比这更窄 —— 画布就宽过容器，右边一列连同坐标被切掉。
**棋盘宁可小，也不能超出容器。**

### 一次代码整理

屏蔽非法点的那一行原本有三份：`net.py` 里的 `masked_policy`、`cli/engine.py` 的单局
与批量两条路径各抄了一遍。这行是**零搜索部署路径上唯一的非模型逻辑**（技术报告第 10 章
的论点），不该有三份定义 —— 哪天有人只改了其中一处，另外两处会静默地走另一套规则。
合并到一处，并改名为 `masked_logits`：它返回的是 logits 不是概率，
原名会让人再 softmax 一次。

### 静默失败 #18：续训时回放池被按 actor 装偏了

renju15c 第三次续训后，策略 top1 从 68.5% 掉到 63.1%、超出目标熵从 0.43 涨到 0.58，
而且 850 步没有回来。

`restore_from_shards()` 按**文件名**排序装载分片，而文件名是 `actorN_XXXXXXXX.npz`
—— 排序会先按 actor 分组、再按时间。磁盘分片总量一旦超过窗口容量就触发截断，
倒序装载于是变成：

    actor0: 装入   0 / 磁盘上共 341
    actor1: 装入 144 / 磁盘上共 344
    actor2: 装入 345 / 磁盘上共 345   ← 连最旧的都装进来了

回放池成了「actor2 的全部历史 + actor1 最新的 42% + actor0 一条没有」，
而不是「全体最近的 400 万条」。actor2 的老数据来自更弱的教师，策略目标质量下降。

**不报错，数据也全都是合法样本**，只是悄悄换成了一批更旧的。

证据链闭合在三点上：只有策略两项掉、价值与威胁纹丝不动（后两者标签由规则导出，
与教师强弱无关）；前两次续训完全没有阶跃 —— 那时磁盘分片总量还没超过容量，
不触发截断。三个数据点全部吻合。

值得记的是**我的第一个假设是错的**：我先猜"续训导致回放池整体变旧"，
结果被前两次续训的数据直接证伪（第二次的超出量甚至一模一样，0.798 → 0.798）。
是那次证伪把我推到真因上的。**先去找能证伪假设的数据，比继续为假设找支持有效得多。**

修法：按分片序号（时间）排序而不是文件名。补了回归测试 —— 造 3 个 actor × 4 轮分片、
容量只装得下 6 片，断言装回来的必须是最新两轮的三个 actor 各一片。

## 2026-08-03 — 更长的一轮（renju15c，48,000 步）

用同一套方案把训练量从 3 万提到 5 万步，为的是检验第 12 章那条否定性结论 ——
它原本只在一次训练内部、约 1.5 万步的窗口里成立，可以被质疑"窗口太短"或
"学习率没衰减够"。

实际跑到 step 47,958（终点 checkpoint），中途 4 次中断、跨三台物理机，
trainer 累计墙钟 21.3 小时，自博弈 132 万局。最后 1,800 步没补跑：
学习率那时已到 1.07e-4、逼近下限，不值得为此再占一轮机器。

**结果是结论更硬了。** 这一轮训练侧全面更好（超出目标熵 0.391 对 0.427，
全项目最低）、绝对棋力也确实更强（直接对局 113:85 赢下第一轮），
而头号指标一点没动、甚至略低（各档差 6~7 个百分点，噪声 3.5 个百分点，
"没变好"可以确认，"更差"不能确认）。两条质疑都被排除。

关键在于**这个指标是相对的**：分母是模型自己的 MCTS。网络变强，教师同样变强。
训练让学生和老师一起进步，却没有缩短两者的距离 —— 而部署时我们只有学生。

细节写进了[附录](docs/appendix-training-run.md)，第 12 章也补了这段更强的证据。

### 顺带纠正一个旧解释

此前曾把自博弈里黑方的压倒性优势归因于"自由开局下先手的理论优势"。
这一轮的自博弈黑白比例在**同一次训练内部**就摆了两趟：早期白胜 71% →
中期几乎均衡 → 末段黑胜 69.9%。一个理论性质不会在一次训练里来回翻转，
这个解释站不住。更可能是自博弈的自我强化，而它的落点会来回震荡。

### 分片回收：一条从未执行过的配置

`_prune_shards()` 按 `self.shard_prefix` 过滤，而多卡编排下 trainer 根本不写分片
（自博弈在 actor 侧，actor 又都配 `keep_shards=0`）—— 一个文件都匹配不上，
**回收从未执行过**，磁盘一路涨到 1,478 片 17 GB。配置里那行 `disk_keep_shards`
完全是死的。

这正是 #18 从潜伏变成爆发的诱因：分片总量一旦超过回放池容量就触发装载截断，
截断才让排序的错误显形。**两个缺陷叠在一起才出事，单独任何一个都不会。**

第二个缺陷是保留量本身也不够：400 片 × 8192 条 = 328 万 < 回放池容量 400 万，
即使回收正常工作，续训时磁盘分片也凑不够、回放池永远恢复不到容量。

修法：回收跨所有写入方、按分片序号（时间）删最旧的、并打日志说明删了几个；
保留量提到 800 片。补了回归测试断言"回收确实执行、删的是最旧的、没有按 actor 删偏"。

### 收掉两个自己踩过的运维缺口

都是文档里明确标着「未做」、而且我在这三轮训练里各踩过一次的。

**① 训练跑满后 actor 不知道，继续空烧 GPU。** 踩过两次，每次白烧三张卡。
actor 是独立进程，trainer 退出它无从知晓，而 `launch_training.sh` 的 stop 是纯手动的。
修法用两侧本来就唯一的耦合方式 —— 文件系统：trainer 跑满后落一个 `<run-dir>/DONE`，
actor 每轮检查它并收摊。要紧的是**开训时必须先清掉上一轮的哨兵**，
否则调大 `max_steps` 再续训时 actor 会一起步就退出、trainer 永远等不到数据。

**② `min_positions_to_start` 是绝对值门槛。** 5 万条配 400 万的容量，形同虚设 ——
这正是 #15 的帮凶：分片没装回来时只攒到容量的 1.25% 就开训，
网络迅速过拟合那一小撮样本，而三项训练指标反而冲到全程最佳。

以前一直没修，是因为不知道该拿什么当基准。这次想明白了：**checkpoint 里本来就存着
中断前的回放池大小**，续训后一比对就知道恢复得对不对。于是做两件事 ——
恢复量不足一半时大声告警（并在告警里点明"指标会变好看、极易被误当成进展"，
因为这正是它最危险的地方）；开训门槛也按中断前的一半抬高，宁可等 actor 把池子填回来。

这条也算给 #15 补上了真正的防线：原来只有事后从曲线里认出来，现在启动时就会喊。


## 2026-08-03 补记 — 只保留最强的一版

renju15c（48,000 步）直接对局赢下此前那版，成为保留下来的唯一模型；
更早那些轮次的权重与日志已全部清理，文档里由 renju15c 全面接替其位置。

技术报告随之做了一次校准，值得记的是**哪些该改、哪些不该改**：

- **两份附录合并成一份**，主体换成保留下来的这一轮，并补齐了它自己的禁手控制、
  规模产出等此前只有旧轮次有的小节。
- **正文里的"当前状态"类数字全部换新**：棋力标定（200:0、199:1）、
  等效模拟数（不到 2 次）、威胁识别（98.1%）。
- **历史测量保留，但标明来源**。第 12 章那条学习率衰减实验、以及头号指标的
  多点扫档，都来自已清理的那一轮 —— 它们是真实记录，不该因为产物被删就抹掉，
  但必须写清"产物已不存在、无法重测"，否则读者会以为还能复现。

顺带修掉一处**我自己写下的过度解读**：原来写"从 1 万步到 3 万步这个指标确实
从 <2 走到了 4~8"，语气偏肯定。跑到 48,000 步时它又回到了 <2，而各档差距
（6~7 个百分点）本来就在噪声量级（每档 200 局的二项标准差 3.5 个百分点）上。
现在改成"它至多以远慢于拟合精度的速度在动，慢到在任何单次实验里都量不出可靠趋势"，
并提醒读者别把这几个读数当成一条平滑上升的曲线。

**证据强度不该随讲述的方便而升级。**

### 移除命令行对战

`gomoku-instinct play` 及其光标模式整体删除：`cli/play.py`、`cli/cursor_play.py`、
`cli/keyboard.py`、`tests/test_cursor_input.py`，以及 `main.py` 里的 play 子命令。
`render.py` 里只有它用的 `label_to_move`、`CoordinateError`、`render_compact`
也一并去掉。

**这原本是项目最初的硬性要求之一**（"写一个命令行的工具，用来和人对战"），
后来网页版在功能上完全覆盖了它，遂由用户决定移除。上面 M7 那两节记的是当时的开发过程，
保留不动 —— 那些事确实发生过，包括 `tty.setraw` 默认 `TCSAFLUSH` 会丢掉已排队输入
那个坑，以及"先量再改"撤掉禁手缓存那次自我纠正。**代码删了，教训不删。**

删的时候要分清哪些是它专有、哪些是共用的：

- `cli/engine.py`（`InstinctPlayer`）**必须留** —— 它是零搜索约束的落地点，
  网页版和 `search_gap.py` 都依赖它；
- `cli/render.py` 的 `move_to_label` 网页版在用、`render_board` 被 `show_game.py` 用，
  也要留；
- `cli/main.py` 还托管着 serve / arena / show 三个子命令，只能摘掉 play。

删完 `cli/` 从 959 行降到 455 行。

### 一个并发竞争：多卡启动时把编译缓存删到一半

起 renju15d 时 launch 脚本报告四个容器全部启动、退出码 0，但实际只有三个在跑
—— **actor2 启动即退出，没有任何人报告**。

actor2 的日志里只有一行自相矛盾的话：

    检测到项目根目录已变更（原 /home/.../GomokuInstinct），清理编译缓存……
    rm: cannot remove '.../build/torch_extensions/gi_core': Directory not empty

"原路径"和当前路径一模一样，却判定"变更了"。原因是 `docker_run.sh` 里那段检查
**cat 了两次**：第一次比较时读到的是另一个容器正在 `> $ROOT_MARKER`
（先截断再写）的空内容，判定不等；第二次打印消息时对方已写完，于是打印出正确路径。
四个容器几乎同时跑到这里，撞上是迟早的事。

**万幸 rm 失败了。** 它撞上其它容器正在写同一批目录，返回非零 —— 编译缓存因此保住，
而 `set -euo pipefail` 把 actor2 带走了。**如果 rm 成功，会把另外三个正在跑的容器
的编译缓存删掉。**

三处修法：

1. 内容已经正确就**根本不写**，从源头消除截断窗口；
2. 真要更新时先写临时文件再原子 rename；
3. 清理失败不再中止脚本 —— 缓存清不掉最多是慢一点，不该让容器起不来。

但最要紧的是第四处，它和这个竞争无关：**`launch_training.sh` 现在会数容器**。
nohup 的失败不会传回来，脚本照样退 0；少一个 actor，训练用 2/3 的产能一路跑完，
所有日志看起来都正常，只有事后从吞吐量反推才可能发现。现在数不够就直接报错退出。

这条教训和图鉴里那些是同一类：**"启动了"和"在跑"是两回事，中间那个缺口不会自己喊。**

### 尾段重搜：两趟走

上一版「尾段窗口」其实名不副实：它保留抽签、只在终局后按手数过滤，
所以最后 32 手里只收得到恰好抽中完整搜索的那约四分之一（8 条/局，而不是 32 条）。

用户直接指出了这一点：「自我对弈一局到分出胜负为止，每一步的局面不都已经确定了吗？
取最后 20 步送入回放池也是很自然的事。」

**他是对的，而我的三个选项里没有一个是他真正想要的那个。** 我把"抽签决定搜索深度"
和"抽签决定要不要入池"在脑子里绑死了 —— 其实它们本来就是两回事，
而且 `Sample` 结构里那个恒为 1 的 `searched` 字段说明原作者当初就想过要拆开。

真正的约束只有一条：**局面事后都在，但策略目标是当时那次搜索的副产品，事后回不去。**
所以要"最后 N 手一个不漏且都是干净目标"，只能分两趟：

    落子阶段：照常走完，一条样本都不产
    终局之后：回退到 T-N，逐个局面用满 sims 重搜，那次搜索的分布才是训练目标

算力反而更划算：每局 `局长×175 + N×400`，N=20、局长 39 时约 595 次模拟/样本，
比边下边采的 700 还便宜，而且每条都是满搜索的目标、每局拿满 N 条而不是 0.25×局长 条。

实现上从外面看，一个 slot 在重搜时只是在下一局特别的棋 —— 每轮仍然一次下潜，
**批大小恒定不变**，和落子阶段完全同构，没有破坏定长批的设计。

三个容易出错的地方：

1. **`undo()` 会弹掉 `moves_`**，回退前必须先把着法列表拷出来，否则重搜时
   取不到「这一手之后对手怎么应」，`next_move` 标签就没了。动手前专门核实过。
2. **重搜不加 Dirichlet 噪声** —— 噪声是为了让自博弈去试没试过的着法，
   重搜只是评估一个已确定的局面。顺带一提这比原来更干净：
   边下边采的训练目标是从加过噪的根节点搜出来的。
3. **认输局跳过重搜** —— 它没走到真正的终局，胜负是价值头判的，
   拿它的残局当高质量目标说不通。

旧的 `keep_last_plies` 连同 `samples_dropped` 一起删掉，没有留两个重叠的旋钮 ——
配置里放两个做类似事情的开关，正是本项目一直在防的「读配置的人以为它在生效」。
代价是 renju15d 的配置无法再复现，其结论与权重已保留。

9x9 冒烟（同一套设置，只改窗口）：

    窗口 关 -> 11.5 条/局（期望 0.25×45.5 = 11.4），plies_remaining ∈ [1, 68]
    窗口 20 -> 19.3 条/局（期望 20.0），            plies_remaining ∈ [1, 20]
    窗口  6 ->  5.4 条/局（期望 6.0），             plies_remaining ∈ [1, 6]

对比旧方案在窗口 20 下只能拿到约 5 条/局 —— 「一个不漏」这次才真正做到。

### 尾段采样的三轮对照实验

同为 1 万步、同学习率调度、同网络结构，唯一差别是样本从哪来：

| 采样方式 | 对贪心基线 | 价值准确率 | 超出目标熵 | 目标熵 | 等效模拟数 |
|---|---|---|---|---|---|
| 全局均匀（基线） | 76.8% | 78.4% | 0.861 | 1.387 | — |
| 尾段窗口 32（边下边采 + 事后过滤） | 83.5% | 81.3% | 0.908 | 1.351 | < 2 |
| **尾段重搜 20（两趟走）** | **87.8%** | **85.1%** | **0.430** | **1.074** | **≈ 2** |

外加直接对局：两趟走 118:82 胜过旧窗口（Elo +63，约 2.6 sd）。

三项独立测量方向一致、单调递增。**这个思路有效。** 核心机制（残局的价值标签方差
最小）在价值准确率上体现得最直接 —— 这一项受目标熵影响最小，+6.7pp。

两处必须写清的保留：

**① 超出目标熵那一列不能单独拿来比。** 两趟走的目标熵低得多（1.074 vs 1.387），
因为残局候选点少、访问分布本来就尖锐，加上重搜不加 Dirichlet 噪声，目标进一步集中。
**目标越尖锐越好拟合** —— 那 0.430 里有多少是模型更强、多少是任务更简单，这张表分不出来。

**② 头号指标的「≈2」受一个混淆项影响**：这个指标的分母是模型自己的 MCTS。
两趟走那版网络整体更弱（它只赢旧窗口 59%，而旧窗口被 4.8 万步的基线打成 17%），
网络弱则它的老师也弱，差距自然小。「策略吸收了更多搜索能力」和「搜索在弱网络上
本来就发挥不出来」，现有数据分不开。

**一处退步**：自博弈禁手告负从 0.26% 升到 0.64%（几万局统计，不是噪声）。
禁手召回两轮相近（81.4% vs 81.2%），所以不是预测头的问题，
而是策略头在有禁手风险的点上选点更激进。原因未查明。

另外记一个和这次实验无关、但值得留下的观察：**对局长度是非单调的**。
随机初始化时约 47 手/局（谁都连不成五），1.4 万步左右降到约 30 手（会攻不会守，速胜），
4 万步后回到约 50 手（攻守兼备）。我曾拿中期那个低点当起点，据此预测"窗口早期不丢样本"，
结果一开局丢弃率就是 37.8%。**中间值不能当端点用。**
